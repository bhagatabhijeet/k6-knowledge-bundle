// Generated by Grafana k6 Studio (1.13.0) from a 3-request QuickPizza recording — unedited export.
// Run it with:  k6 run studio-generated-example.js   (default load: 20 VUs for ~5.5 min; use --stage 3s:2,3s:0 for a quick try)

import { group, sleep, check } from "k6";
import http from "k6/http";
import execution from "k6/execution";

export const options = {
  stages: [
    { target: 20, duration: "1m" },
    { target: 20, duration: "3m30s" },
    { target: 0, duration: "1m" },
  ],
  thresholds: {
    http_req_duration: [{ threshold: "p(95)<400", abortOnFail: true }],
  },
};

export default function () {
  let params;
  let resp;
  let match;
  let regex;
  let url;
  const correlation_vars = {};

  group("Home page", function () {
    params = {
      headers: {
        Accept: `text/html,application/xhtml+xml`,
      },
      cookies: {},
    };

    url = http.url`https://quickpizza.grafana.com/`;
    resp = http.request("GET", url, null, params);

    check(resp, { "status equals 200": (r) => r.status === 200 });
  });
  sleep(1);
  group("Get pizza data", function () {
    params = {
      headers: {
        Accept: `application/json`,
      },
      cookies: {},
    };

    url = http.url`https://quickpizza.grafana.com/api/quotes`;
    resp = http.request("GET", url, null, params);

    check(resp, { "status equals 200": (r) => r.status === 200 });

    params = {
      headers: {
        Accept: `application/json`,
      },
      cookies: {},
    };

    url = http.url`https://quickpizza.grafana.com/api/names`;
    resp = http.request("GET", url, null, params);

    check(resp, { "status equals 200": (r) => r.status === 200 });
  });
  sleep(1);
}

